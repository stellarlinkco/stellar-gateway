name: Release notes

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - name: Generate release notes
        uses: googleapis/release-please-action@v4
        with:
          release-type: rust
